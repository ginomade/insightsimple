{
  "name": "insightsimple-site",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "dev": "netlify dev --dir=. --functions=netlify/functions",
    "preview": "netlify deploy --dir=. --functions=netlify/functions",
    "deploy": "netlify deploy --prod --dir=. --functions=netlify/functions"
  },
  "devDependencies": {
    "netlify-cli": "^17.0.0"
  }
}
